// Get DOM elements
const backBtn = document.getElementById('backBtn');

// Back button click - redirect to upload page
backBtn.addEventListener('click', () => {
    window.location.href = 'upload_html.html';
});